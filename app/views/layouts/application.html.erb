<!DOCTYPE html>
<html>
  <head>
    <title>Coordinator</title>
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_include_tag 'application', 'data-turbolinks-track': 'reload' %>
  </head>

  <body>
    <header>
      <nav class="background navbar navbar-expand-lg navbar-dark">
        <div class="container">
          <%= link_to root_path do %>
            <%= image_tag 'logo_origin.png', :size => '200x200' %>
          <% end %>
          <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarNavDropdown" aria-controls="navbarNavDropdown" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
          </button>
          <div class="collapse navbar-collapse" id="navbarNavDropdown">
    　      <% if user_signed_in? %>
              <li>
                <%= link_to "マイページ", user_path(current_user),class: 'fas fa-user nav-link text-light' %>
              </li>
              <li>
                <%= link_to "ユーザー一覧", users_path,class: 'fas fa-users nav-link text-light' %>
              </li>
              <li>
                <%= link_to "投稿一覧", root_path,class: 'fas fa-paste nav-link text-light' %>
              </li>
              <li>
                <%= link_to "ログアウト", destroy_user_session_path, method: :delete,class: 'fas fa-sign-out-alt nav-link text-light' %>
              </li>
            <% else %>
              <li>
                <%= link_to "新規登録", new_user_registration_path,class: 'fas fa-user-plus nav-link text-light' %>
              </li>
              <li>
                <%= link_to "ログイン", new_user_session_path,class: 'fas fa-sign-in-alt nav-link text-light' %>
              </li>
              <li>
                <%= link_to "ユーザー一覧", users_path,class: 'fas fa-users nav-link text-light' %>
              </li>
              <li>
                <%= link_to "投稿一覧", root_path,class: 'fas fa-paste nav-link text-light' %>
              </li>
            <% end %>
            <%= form_with url: search_path, method: :get, local: true do |f| %>
              <%= f.select :range, options_for_select([['ユーザー'], ['タグ']]) %>
              <%= f.text_field :keyword, value: @keyword %>
              <%= f.submit "検索" %>
            <% end %>
          </div>
        </div>
　　　</nav>
    </header>
    <%= yield %>
    <footer>
      <nav class="background navbar navbar-dark">
        <div class="text-light mx-auto">koushikoushikoushi</div>
      </nav>
    </footer>
  </body>
</html>
