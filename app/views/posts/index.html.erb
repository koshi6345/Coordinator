<div class="container">
  <div clsss="row">
    <div class="col-12">
      <% @posts.each do |posts| %>
        <p>
          <%= link_to post_path(posts) do %>
            <%= attachment_image_tag(posts, :image, :fill, 50, 50, fallback: "no-image-icon.jpg") %>
          <% end %>
        </p>
        <p>
         <%= posts.introduction %>
        </p>
      <% end %>
    </div>
  </div>
</div>