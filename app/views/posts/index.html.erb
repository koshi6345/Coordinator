<%= link_to "新規投稿フォーム", new_post_path %>
<% @posts.each do |posts| %>
  <p>
    <%= link_to post_path(posts) do %>
      <%= attachment_image_tag(posts, :image, :fill, 50, 50, fallback: "no-image-icon.jpg") %>
    <% end %>
  </p>
  <p>
   <%= posts.introduction %>
  </p>
<% end %>