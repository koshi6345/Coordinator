<div class="container">
  <div clsss="row">
    <div class="col-12">
      <%= form_with model:@user, local:true do |f| %>
        プロフィール画像 <%= f.attachment_field :image %>
        名前 <%= f.text_field :name %>
        自己紹介文 <%= f.text_area :introduction %>
        <%= f.submit %>
      <% end %>
    </div>
  </div>
</div>